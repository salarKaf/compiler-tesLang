funk main() <null> {

    a :: int = 5
    b :: int = 3;

    if [[ a > 3 && b < 4 ]] begin
        print(100);
    end

    if [[ a < 2 || b < 10 ]] begin
        print(200);
    end

    if [[ a < 2 && b < 10 ]] begin
        print(999);
    end

    return;
}
